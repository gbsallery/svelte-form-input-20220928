import { type Writable } from "svelte/store";
import type { Session } from "./types.js";
export declare const signOut: (redirect?: string) => Promise<void>;
export declare const refreshTokens: (refreshToken: string) => Promise<{
    refresh_token: any;
    access_token: any;
}>;
export declare const getSession: () => Writable<Session>;
export declare const handleSilentRefresh: (errorHandler?: () => void) => void;
