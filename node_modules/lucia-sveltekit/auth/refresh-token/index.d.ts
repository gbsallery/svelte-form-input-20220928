import { AccessToken, RefreshToken } from "../../utils/token.js";
import type { Context } from "./../index.js";
declare type RefreshTokens = (refreshToken: string, fingerprintToken: string) => Promise<{
    access_token: AccessToken;
    refresh_token: RefreshToken;
}>;
export declare const refreshTokensFunction: (context: Context) => RefreshTokens;
export {};
