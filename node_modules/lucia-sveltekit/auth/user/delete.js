export const deleteUserFunction = (context) => {
    const deleteUser = async (userId) => {
        await context.adapter.deleteUserRefreshTokens(userId);
        await context.adapter.deleteUser(userId);
    };
    return deleteUser;
};
