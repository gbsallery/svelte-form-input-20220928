import { AccessToken, FingerprintToken, RefreshToken } from "./token.js";
import type { DatabaseUser, User } from "../types.js";
import type { Context } from "../auth/index.js";
export declare const createAccessToken: (user: User, fingerprintToken: string, context: Context) => Promise<AccessToken>;
export declare const createRefreshToken: (userId: string, fingerprintToken: string, context: Context) => Promise<RefreshToken>;
export declare const createFingerprintToken: (context: Context) => FingerprintToken;
export declare const getAccountFromDatabaseData: (databaseData: DatabaseUser) => {
    user: User;
    hashed_password: string | null;
    identifier_token: string;
};
