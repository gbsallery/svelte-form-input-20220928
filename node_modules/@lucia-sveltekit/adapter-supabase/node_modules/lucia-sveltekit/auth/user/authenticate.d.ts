import type { ServerSession } from "../../types.js";
import type { Context } from "../index.js";
declare type authenticateUser = (authId: string, identifier: string, password?: string) => Promise<ServerSession>;
export declare const authenticateUserFunction: (context: Context) => authenticateUser;
export {};
