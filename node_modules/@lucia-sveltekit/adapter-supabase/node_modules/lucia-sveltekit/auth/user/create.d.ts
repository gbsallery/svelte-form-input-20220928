import type { ServerSession } from "../../types.js";
import type { Context } from "../index.js";
declare type CreateUser = (authId: string, identifier: string, options: {
    password?: string;
    user_data?: Lucia.UserData;
}) => Promise<ServerSession>;
export declare const createUserFunction: (context: Context) => CreateUser;
export {};
