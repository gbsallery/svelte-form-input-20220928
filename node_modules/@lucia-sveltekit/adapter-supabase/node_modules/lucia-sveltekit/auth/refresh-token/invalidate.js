export const invalidateRefreshTokenFunction = (context) => {
    const invalidateRefreshToken = async (refreshToken) => {
        await context.adapter.deleteRefreshToken(refreshToken);
    };
    return invalidateRefreshToken;
};
